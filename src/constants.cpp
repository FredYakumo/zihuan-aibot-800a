#include "constants.hpp"

// Define the BUILD_VERSION_STRING constant using the CMake-provided macro
const char *const BUILD_VERSION_STRING = BUILD_VERSION;

// Define the DREPOS_ADDR constant using the CMake-provided macro
const char *const DREPOS_ADDR_STRING = DREPOS_ADDR;
